version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    executor:
      name: node/default
      tag: '10.4'
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: sudo npm install yarn -g
            - run: yarn 
            - run: yarn watch
workflows:
    build-and-test:
      jobs:
        - build-and-test
