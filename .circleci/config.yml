version: 2.1
orbs:
  node: circleci/node@1.1.6
jobs:
  build-and-test:
    executor:
      name: node/default
      tag: '10.4'
    steps:
      - checkout
      - node/with-cache:
          steps:
            - run: sudo apt-get update
            - run: sudo apt-get install libx11-dev libxkbfile-dev libsecret-1-dev
            - run: sudo npm install yarn -g
            - run: sudo npm install node-abi -g
            - run: sudo yarn install --ignore-engines
            - run: yarn watch
workflows:
    build-and-test:
      jobs:
        - build-and-test
